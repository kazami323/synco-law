# App
DEBUG=true

# Security — ОБЯЗАТЕЛЬНО заменить в production (openssl rand -hex 32)
SECRET_KEY=change-me-in-production

# Database (локально Docker пробрасывает 5432 на localhost)
DATABASE_URL=postgresql+asyncpg://legal_user:secure_password@localhost:5432/legal_workspace

# Infrastructure
REDIS_URL=redis://localhost:6379/0
ELASTICSEARCH_URL=http://localhost:9200
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=contracts

# AI
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-4-8

# External APIs
LEX_UZ_API_KEY=
